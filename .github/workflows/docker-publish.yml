name: docker-publish

on:
  workflow_run:
    workflows: [e2e]
    types: [completed]

permissions:
  actions: read
  contents: read
  packages: write

jobs:
  publish:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: web
            hub: nguyenson1710/travelai-web
            ghcr: ghcr.io/jasontm17/travelai-web
            context: ./web
          - service: api
            hub: nguyenson1710/travelai-api
            ghcr: ghcr.io/jasontm17/travelai-api
            context: ./api
          - service: identity
            hub: nguyenson1710/travelai-identity
            ghcr: ghcr.io/jasontm17/travelai-identity
            context: ./identity
          - service: ai
            hub: nguyenson1710/travelai-ai
            ghcr: ghcr.io/jasontm17/travelai-ai
            context: ./ai
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Verify required checks passed for the same commit
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          for attempt in $(seq 1 30); do
            all_success=true
            for workflow in ci.yml trivy.yml codeql.yml gitleaks.yml; do
              conclusion="$(gh run list --workflow "$workflow" --commit "$HEAD_SHA" --limit 1 --json conclusion --jq '.[0].conclusion // ""')"
              if [ "$conclusion" = "success" ]; then
                continue
              fi
              all_success=false
              if [ -n "$conclusion" ] && [ "$conclusion" != "in_progress" ] && [ "$conclusion" != "queued" ]; then
                echo "$workflow for $HEAD_SHA failed with conclusion: $conclusion"
                exit 1
              fi
            done
            if [ "$all_success" = "true" ]; then
              exit 0
            fi
            sleep 20
          done
          echo "Required checks for $HEAD_SHA did not complete successfully within 10 minutes."
          exit 1

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: ${{ env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != '' }}
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Select registries
        id: registries
        shell: bash
        run: |
          {
            echo 'images<<EOF'
            echo '${{ matrix.ghcr }}'
            if [ -n "$DOCKERHUB_USERNAME" ] && [ -n "$DOCKERHUB_TOKEN" ]; then
              echo '${{ matrix.hub }}'
            fi
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.registries.outputs.images }}
          tags: |
            type=raw,value=latest
            type=sha,format=long
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
          labels: |
            org.opencontainers.image.source=https://github.com/JasonTM17/VN_TravelAI
            org.opencontainers.image.title=travelai-${{ matrix.service }}
            org.opencontainers.image.description=TravelAI ${{ matrix.service }} service
            org.opencontainers.image.licenses=MIT

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            BUILD_SHA=${{ github.event.workflow_run.head_sha }}
